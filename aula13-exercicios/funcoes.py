def area(l,c) :
    areaTerreno = l * c
    print(f'A area do terreno é {areaTerreno}m²')

largura = float(input('Qual é a largura do terreno? '))
comprimento = float(input('Qual é o comprimento do terreno? '))
area(largura,comprimento)